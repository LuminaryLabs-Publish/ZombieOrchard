# Turn ledger: ZombieOrchard world viewport and camera coverage

**Timestamp:** `2026-07-18T08-39-41-04-00`  
**Branch:** `main`  
**Selection:** oldest synchronized eligible Publish repository

## Completed

- Compared all 11 `LuminaryLabs-Publish` repositories against `LuminaryLabs-Dev/LuminaryLabs/repo-ledger`.
- Excluded `LuminaryLabs-Publish/TheCavalryOfRome`.
- Confirmed all ten eligible repositories had a central ledger, root `.agent` state and a synchronized documented head.
- Selected and modified only `LuminaryLabs-Publish/ZombieOrchard`.
- Identified the complete interaction loop, all active domains, all 27 implemented kits and every offered service.
- Audited world bounds, player bounds, Canvas2D viewport sizing and world-to-screen projection.
- Documented one proposed 20-surface world viewport and camera authority.
- Added architecture, render, gameplay, interaction, world-projection, deployment and central-sync audit entries.
- Refreshed all required root `.agent` files.
- Changed no runtime, gameplay, rendering, test, build or deployment code.
- Created no branch or pull request.

## Main finding

`world-canvas-render-kit` always places the world origin at the center of the current canvas and adds raw world coordinates at scale `1`. The playable world is 720 by 560 units, but the renderer has no camera, fit scale or visibility policy. Valid player, tree, apple and pest positions can therefore be outside the visible canvas on narrow or short viewports.

## Proposed authority

`zombie-orchard-world-viewport-camera-coverage-authority-domain`

## Validation

Source inspection completed. Direct checkout was blocked by DNS resolution for `github.com`; executable tests, browser viewport fixtures, artifact smoke and Pages smoke were not run.
