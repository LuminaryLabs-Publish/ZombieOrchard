# Turn ledger: 2026-07-11T23-48-14-04-00

## Repository

`LuminaryLabs-Publish/ZombieOrchard`

## Goal

Audit composite command execution and document the authority needed for atomic, idempotent, single-publication, frame-correlated results.

## Work completed

- Compared the 10 current Publish repositories with the central ledger.
- Excluded `TheCavalryOfRome`.
- Selected only `ZombieOrchard`.
- Read runtime dispatch, scoped interfaces, composition, presets, gameplay domains, browser bindings and proof scripts.
- Identified 27 implemented kits and their offered services.
- Documented hidden child rejection, duplicate notification and partial multi-domain commit paths.
- Added architecture, render, gameplay, interaction, command-transaction and deploy audits.
- Refreshed required root `.agent` files and the kit registry.

## Main finding

```txt
one browser intent
  -> parent activation
  -> nested child command
  -> child mutation + publication
  -> child result ignored
  -> parent success + second publication
```

The command system needs a full participant plan, prepare/commit/rollback, idempotency and one aggregate result before replay or persistence can be trustworthy.

## Scope confirmation

```txt
runtime source changed: no
branch created: no
pull request created: no
Publish repos modified: ZombieOrchard only
push target: main
```