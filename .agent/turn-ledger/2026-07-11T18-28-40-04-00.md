# Turn ledger — 2026-07-11T18-28-40-04-00

## Summary

Selected `LuminaryLabs-Publish/ZombieOrchard` through the oldest eligible documented-repository rule. Audited the single-graph lifecycle and found that Entry, New Game, Play, Outcome and Title are interface routes only; no route creates or retires a run.

## Plan ledger

**Goal:** record the source-backed fresh-run defect and define the runtime-session authority required before restart, pause, replay or persistence work.

- [x] Compared all ten accessible Publish repositories.
- [x] Excluded `TheCavalryOfRome`.
- [x] Confirmed nine eligible central ledgers and root `.agent` folders.
- [x] Selected only `ZombieOrchard`.
- [x] Read `src/start.js`, `src/game.js`, `src/kits/runtime.js`, `src/kits/composition.js`, `src/kits/game-domains.js`, `src/presets/orchard-preset.js`, both renderers and `tests/smoke.mjs`.
- [x] Identified interaction loop, domains, kits and services.
- [x] Added runtime-session architecture and system audits.
- [x] Refreshed required root `.agent` state.
- [x] Pushed directly to `main` with no branch or pull request.
- [ ] Runtime implementation and lifecycle fixtures remain future work.

## Finding

```txt
ended run
  -> Outcome
  -> Title routes to Entry only
  -> Play or New Game routes to the same graph
  -> ended remains true
  -> next tick routes back to Outcome
```

Resources, pressure, orchard apples, construction, roster, inventory, day, pests, score and player condition also survive because they remain inside the same domain closures.

## Required boundary

```txt
zombie-orchard-runtime-session-authority-domain
  -> fresh graph staging
  -> runtime/run/session identity
  -> lifecycle admission
  -> atomic run commit or rollback
  -> route/run binding
  -> stale callback and command rejection
  -> first-run-frame acknowledgement
  -> ordered disposal
```

## Validation

Documentation-only run. No runtime, dependency, rendering, package-script or deployment changes. Existing smoke proof does not cover failure, Title, New Game, restart, fresh state, stale callbacks or disposal.