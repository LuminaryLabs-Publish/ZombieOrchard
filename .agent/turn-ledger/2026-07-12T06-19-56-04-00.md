# Turn ledger: ZombieOrchard

**Timestamp:** `2026-07-12T06-19-56-04-00`

## Goal

Synchronize the newer repo-local canvas render-surface audit with the required root audit state and central ledger without changing runtime behavior.

## Completed

- Compared the 10 accessible Publish repositories and excluded `TheCavalryOfRome`.
- Confirmed all nine eligible repositories are centrally tracked and contain root `.agent` state.
- Selected only `ZombieOrchard` because its `06-11-18` repo-local canvas audit was newer than central tracking.
- Re-read `index.html`, `src/styles.css`, `src/start.js`, `src/renderer/world-canvas.js` and the current audit set.
- Confirmed one literal simulation step per RAF, per-frame canvas buffer assignment, CSS/physical resolution conflation and fixed-coordinate projection.
- Preserved the complete 27-kit and service inventory.
- Added a fresh timestamped architecture and system audit family.
- Refreshed required root audit files and registry.

## Main finding

```txt
CSS viewport observation
  -> ambient read during render
  -> unconditional buffer assignment
  -> no DPR or pixel-budget policy
  -> no surface revision or allocation result
  -> raw fixed-world projection
  -> no visible-frame receipt
```

## Validation boundary

Documentation only. Runtime source, package scripts, dependencies, rendering, gameplay and deployment were not changed. Browser viewport, DPR, pixel-budget, world-fit and Pages fixtures remain unavailable.