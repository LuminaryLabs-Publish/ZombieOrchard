# Turn ledger: ZombieOrchard runtime session instance authority

## Timestamp

```txt
2026-07-11T13-41-23-04-00
```

## Plan ledger

**Goal:** document the missing owner for graph construction, run identity, lifecycle commands, RAF, browser bindings, render resources, public capabilities and disposal.

- [x] Selected only `LuminaryLabs-Publish/ZombieOrchard`.
- [x] Excluded `LuminaryLabs-Publish/TheCavalryOfRome`.
- [x] Read the current source and prior clock audit.
- [x] Preserved fixed-step clock authority as the next dependent gate.
- [x] Added a new tracker and timestamped audit set.
- [x] Refreshed the required root `.agent` files.
- [x] Changed no runtime source.
- [x] Pushed directly to `main`.
- [ ] Implement and validate runtime-session authority.

## Finding

The browser creates one mutable engine graph before any run is selected. Play, New Game, Start, Pause, Resume, Title and Outcome change interface routes but do not create, suspend, retire or dispose a run instance. The recursive RAF, delegated click listener, render owners and `window.GameHost` have no retained lifecycle leases.

## Required result

```txt
identified runtime
  -> identified session and epoch
  -> off-line graph construction
  -> atomic authority transfer
  -> one retained RAF
  -> leased listeners/renderers/public host
  -> lifecycle-admitted commands
  -> ordered idempotent disposal
  -> immutable observation and fixture proof
```

## Validation state

```txt
runtime source changed: no
dependencies changed: no
package scripts changed: no
branch created: no
pull request created: no
npm test: not run
npm run build: not run
browser smoke: not run
runtime-session fixtures: absent / not run
```