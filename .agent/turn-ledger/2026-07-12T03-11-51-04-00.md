# Turn ledger: ZombieOrchard fixed-step clock authority

**Timestamp:** `2026-07-12T03-11-51-04-00`  
**Scope:** documentation-only audit  
**Branch:** `main`

## Plan ledger

**Goal:** document and route the refresh-rate-dependent simulation defect into one fixed-step clock authority with exclusive step ownership and executable cadence proof.

- [x] Enumerated all ten accessible Publish repositories.
- [x] Excluded `TheCavalryOfRome`.
- [x] Compared eligible repositories with `LuminaryLabs-Dev/LuminaryLabs`.
- [x] Selected only `ZombieOrchard` as the oldest eligible entry.
- [x] Read `src/start.js`, `src/game.js`, `src/kits/runtime.js`, `src/kits/game-domains.js`, both renderers, package scripts, smoke proof, and current `.agent` state.
- [x] Preserved all 27 implemented kit identities and service groups.
- [x] Added architecture, render, gameplay, interaction, clock-system, and deployment audits.
- [x] Refreshed root `.agent` entrypoints and registry.
- [x] Used `main` only; created no branch or pull request.
- [ ] Runtime implementation and fixtures remain future work.

## Finding

```txt
start.js: one engine.tick(1/60) per RAF
runtime.js: every tick increments elapsed/frame and ticks every domain
GameHost: unrestricted manual tick(dt) reaches the same writer
clock source: display callback count
accumulator: absent
single-writer lease: absent
catch-up/lag policy: absent
visibility suspension: absent
step/frame receipt: absent
```

At 30, 60, and 120 Hz the current source advances approximately 0.5x, 1x, and 2x simulation time per wall second. Pressure, pest movement, contact damage, and stochastic spawn cadence therefore diverge by display cadence.

## Required authority

```txt
zombie-orchard-fixed-step-clock-authority-domain
```

The authority must own wall-time observation, exact fixed steps, step IDs, writer exclusion, catch-up limits, hidden-tab handling, manual stepping, publication barriers, observations, journals, and visible-frame receipts.

## Validation

```txt
runtime source changed: no
dependencies changed: no
package scripts changed: no
render behavior changed: no
deployment changed: no
branch created: no
pull request created: no
npm test: not run
browser cadence smoke: not run
clock fixtures: unavailable
```

No cadence parity, fixed-step determinism, writer exclusivity, hidden-tab safety, or step/frame correlation claim is made.