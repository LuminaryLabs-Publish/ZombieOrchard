# Turn ledger

## Timestamp

```txt
2026-07-11T23-41-55-04-00
```

## Repository

```txt
LuminaryLabs-Publish/ZombieOrchard
```

## Goal

Define the missing public capability gateway between browser diagnostics/tests and the mutable ZombieOrchard runtime graph.

## Completed

- Compared the complete Publish organization inventory with `LuminaryLabs-Dev/LuminaryLabs` repo-ledger entries.
- Excluded `TheCavalryOfRome`.
- Confirmed no eligible repo was new, central-ledger-missing or root-`.agent`-missing.
- Selected ZombieOrchard as the oldest eligible central entry.
- Read `src/start.js`, `src/game.js`, `src/kits/runtime.js`, composition, scoped interface domains, game domains, HTML renderer, package scripts and smoke proof.
- Identified the interaction loop, all domains, all 27 implemented kits and their services.
- Traced the complete `window.GameHost` reachability surface.
- Documented raw-engine, domain-table, direct-API, duplicate-registration and concurrent-step bypasses.
- Defined the public capability gateway DSK, render/read-model contract, interaction admission map and proof gate.
- Refreshed required root `.agent` documents.
- Added the central ledger update and internal change log.

## Main finding

```txt
window.GameHost.engine
  -> mutable ctx
  -> mutable domains table
  -> addKit
  -> unrestricted command
  -> unrestricted tick
  -> subscribe
```

The public host can replace domain authority, call domain APIs directly, advance isolated domains or submit extra full-graph steps beside the browser RAF. It has no capability lease, session identity, allowlist, schema admission, single-writer clock lease, revocation or frame-correlated result.

## Runtime changes

```txt
none
```

## Validation

```txt
npm test: not run
npm run build: not run
browser smoke: not run
public-host fixtures: absent
branch created: no
pull request created: no
push target: main
```
