# ZombieOrchard Turn Ledger

**Timestamp:** `2026-07-08T04-49-40-04-00`

## Selected objective

Clarify the next Market authority implementation slice for `ZombieOrchard` and add a fixture-oriented transaction/result projection audit.

## Why selected

The checked non-excluded Publish repos were already represented in the central ledger and had root `.agent` state. `ZombieOrchard` still had a precise unresolved seam: Market currently routes to `exchange`, but `exchange` only exposes `back`, nested command results are not retained by `interface-composition`, resources have no transaction provenance, and inventory has no purchase intake.

## In scope

```txt
- repo-local .agent documentation
- market authority audit
- follow-up tracker entry
- turn ledger entry
- central LuminaryLabs ledger/change-log update
```

## Out of scope

```txt
- runtime source changes
- game loop rewrite
- renderer changes
- DOM changes
- market implementation
- Playwright/browser testing
- Pages deploy validation
```

## Evidence read

```txt
.agent/current-audit.md
.agent/known-gaps.md
.agent/next-steps.md
.agent/validation.md
.agent/architecture-audit/domain-service-breakdown.md
src/kits/scoped-interface-domains.js
src/kits/composition.js
src/kits/game-domains.js
src/presets/orchard-preset.js
tests/smoke.mjs
```

## Findings

```txt
- interface domain actions already support nested action.command descriptors.
- interface-composition dispatches nested commands but drops the returned result.
- exchange currently only exposes the back action.
- resource-ledger has values, canPay, pay, add, and last state but no transaction journal.
- inventory-runtime has equip only and no purchase intake.
- construction-runtime and roster-runtime already prove resource-payment use, but payments are not transaction-recorded.
- tests/smoke.mjs only covers entry, play transition, and apple existence.
```

## Files changed

```txt
.agent/START_HERE.md
.agent/current-audit.md
.agent/next-steps.md
.agent/market-authority-audit/transaction-result-projection-gate.md
.agent/trackers/2026-07-08T04-49-40-04-00/project-breakdown.md
.agent/turn-ledger/2026-07-08T04-49-40-04-00.md
```

## Validation

Documentation-only pass.

No runtime source files changed.

No local commands were run.

## Next safe ledge

```txt
Market Result Contract + Transaction Projection Fixture Gate
```

Preserve existing `index.html -> src/boot.js -> src/start.js`, active-session HUD, world-canvas renderer, and `snapshot["resource-ledger"].values` compatibility while adding Market command envelopes, accepted/rejected result records, transaction history, nested result propagation, exchange projection, and DOM-free fixtures.