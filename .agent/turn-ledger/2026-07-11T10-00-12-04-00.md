# Turn ledger: ZombieOrchard

## Timestamp

```txt
2026-07-11T10-00-12-04-00
```

## Scope

Documentation-only breakdown of `LuminaryLabs-Publish/ZombieOrchard`, focused on the public capability gateway and internal/debug quarantine boundary.

## Actions completed

- Compared the complete accessible Publish inventory with the central ledger.
- Excluded `TheCavalryOfRome`.
- Selected only `ZombieOrchard` because its repo-local capability documentation was newer than the central ledger.
- Reconfirmed the interaction loop, domains, 27 implemented kits and service inventory.
- Traced direct DOM command dispatch, nested composition dispatch and raw `GameHost` control.
- Defined a composed public capability gateway domain and required fixture gate.
- Refreshed required root `.agent` files and registry.
- Added timestamped architecture, render, gameplay, interaction, gateway and deploy audits.
- Changed no runtime source.
- Pushed only to `main` and created no branch or pull request.

## Finding

```txt
public browser action -> direct engine.command -> discarded result
internal/debug action -> raw GameHost engine/tick -> policy bypass
```

The current capability-reachability plan needs one enforced gateway. Otherwise registry state, disabled affordances and typed results remain advisory while callers can bypass or ignore them.

## Next action

Implement Runtime Session Instance Authority first, then Fixed-Step Clock Authority, then make the gateway consume session, epoch and committed-tick identity.
