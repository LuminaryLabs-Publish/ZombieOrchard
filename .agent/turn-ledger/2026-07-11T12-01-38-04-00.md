# Turn Ledger — ZombieOrchard Fixed-Step Clock Audit

**Timestamp:** `2026-07-11T12-01-38-04-00`

## Goal

Document the current timing loop and define one session-owned fixed-step clock authority without changing runtime behavior.

## Completed

- Compared the full accessible Publish inventory with the central ledger.
- Excluded `TheCavalryOfRome`.
- Selected only `ZombieOrchard` as the oldest eligible documented repository.
- Read `src/start.js`, `src/kits/runtime.js`, `src/kits/game-domains.js`, `src/kits/composition.js`, package scripts and current `.agent` state.
- Confirmed one hard-coded `1/60` simulation update occurs per RAF.
- Confirmed all domains tick once for every engine tick call.
- Confirmed raw manual ticking can advance the same graph between RAF callbacks.
- Defined clock identity, accumulator, pause, catch-up, overrun, manual-step and render-acknowledgement contracts.
- Added the timestamped audit set and refreshed root `.agent` navigation.
- Pushed only to `main`; created no branch or pull request.

## Result

```txt
current:
  RAF count == simulation-step count
  manual tick shares mutation path
  route state does not gate all-domain ticks

target:
  wall-time sample
    -> admitted accumulator
    -> bounded fixed simulation ticks
    -> committed tick receipts
    -> one render frame acknowledgement
```

## Validation

Documentation only. Runtime tests, build, cadence fixtures and browser smoke were not run because the required clock implementation and fixtures do not exist.
