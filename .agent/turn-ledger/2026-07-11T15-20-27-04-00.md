# Turn ledger: ZombieOrchard

## Timestamp

`2026-07-11T15-20-27-04-00`

## Scope

Documentation-only breakdown of fixed-step clock authority.

## Selection

```txt
accessible Publish repositories: 10
eligible non-Cavalry repositories: 9
new or ledger-missing: 0
root .agent missing: 0
selected: LuminaryLabs-Publish/ZombieOrchard
reason: oldest eligible central ledger timestamp
excluded: LuminaryLabs-Publish/TheCavalryOfRome
```

## Work completed

- Read the current runtime-session audit and implementation queue.
- Traced the RAF callback in `src/start.js`.
- Traced delta admission, elapsed/frame mutation and all-domain ticking in `src/kits/runtime.js`.
- Traced pressure growth, random pest spawning, pursuit, damage and failure in `src/kits/game-domains.js`.
- Identified display-cadence-dependent simulation, missing accumulator/catch-up policy, pause/visibility gaps and unrestricted manual tick contention.
- Cataloged the interaction loop, domains, kits and kit services.
- Added architecture, render, gameplay, interaction, clock-authority and deployment audits.
- Refreshed root `.agent` state.
- Changed no runtime source.
- Created no branch or pull request.

## Main finding

```txt
one RAF callback
  -> one hard-coded 1/60 simulation step
```

The runtime does not measure wall time. Simulation speed therefore scales with callback cadence, and manual `GameHost.tick(dt)` can mutate the same graph between automatic callbacks without an exclusive lease.

## Next implementation boundary

```txt
ZombieOrchard Runtime Session Instance Authority
  -> ZombieOrchard Fixed-Step Clock Authority
  -> Pause / 30-60-120 Hz / Stall / Manual-Step Fixture Gate
```

The clock must consume runtime/session identity and lifecycle admission from the earlier Gate 1 design.