# Turn ledger — ZombieOrchard deterministic scenario observation

Timestamp: `2026-07-10T17-18-47-04-00`

## Goal

Document the current ZombieOrchard composition and isolate the smallest proof boundary needed to make command/tick scenarios reproducible, observable, and safe to gate in CI.

## Completed

- Compared the full accessible `LuminaryLabs-Publish` inventory with central ledger and repo-local `.agent` state.
- Excluded `TheCavalryOfRome`.
- Selected only `ZombieOrchard` as the oldest eligible fallback.
- Read boot, start, game factory, runtime, composition, scoped interface, gameplay, preset, render, smoke, package, and Pages workflow sources.
- Reconfirmed the interaction loop, domain set, kit set, and kit services.
- Traced all direct `Math.random()` use in orchard and active-session state.
- Traced command-result, event, snapshot, render-consumption, and GameHost observation boundaries.
- Added a new tracker and system-specific audits.
- Refreshed root `.agent` routing and status documents.
- Changed no runtime source.
- Created no branch or pull request.
- Pushed documentation directly to `main`.

## Main decision

Advance the next safe ledge from Market-only transaction causality to deterministic scenario authority. Market proof remains downstream, but stable replay and whole-state fingerprints require seed ownership, command sequencing, committed-frame observations, and bounded readback first.

## Next safe ledge

```txt
ZombieOrchard Deterministic Scenario Authority
+ Command/Frame Observation Fixture Gate
```

## Runtime validation

```txt
npm test: not run
npm run build: not run
browser smoke: not run
deterministic scenario fixture: unavailable
runtime source changed: no
branch created: no
pull request created: no
```