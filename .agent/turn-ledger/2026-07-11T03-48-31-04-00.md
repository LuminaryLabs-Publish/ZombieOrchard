# Turn ledger — ZombieOrchard

## Run

```txt
2026-07-11T03-48-31-04-00
```

## Goal

Document the dormant save/select surface and define a versioned, migratable, atomic save/load authority boundary.

## Completed

- Compared the full accessible Publish organization inventory with the central ledger.
- Excluded `TheCavalryOfRome`.
- Selected only `ZombieOrchard`.
- Re-read source and proof files.
- Reconfirmed the interaction loop, domains, kits, and services.
- Added a new tracker folder and timestamped audit set.
- Refreshed the required root `.agent` files.
- Changed no runtime source.
- Created no branch or pull request.
- Prepared central ledger and internal change-log synchronization.

## Finding

`session-select` and slot-card rendering exist, but the repository has no persistence owner. The aggregate snapshot is not restorable because domains expose no restore contract and the state lacks schema, content identity, session/tick/seed metadata, random cursors, command receipts, fingerprints, migration, and atomic load semantics.

## Next safe ledge

```txt
ZombieOrchard Runtime Session Instance Authority
+ Start/Reset/Title/Outcome Fidelity Fixture Gate
```

Persistence remains the sixth gate after deterministic replay.
