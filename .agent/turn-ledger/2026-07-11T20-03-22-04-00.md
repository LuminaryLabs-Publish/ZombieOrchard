# Turn ledger — 2026-07-11T20-03-22-04-00

## Summary

Selected `LuminaryLabs-Publish/ZombieOrchard` under the oldest-eligible rule and completed a documentation-only versioned save/load authority audit.

## Plan ledger

**Goal:** reconcile the product’s visible Save Select surface with the actual runtime and define the missing durable continuity boundary.

- [x] Inventory the full Publish organization.
- [x] Compare eligible repositories with central tracking.
- [x] Exclude `TheCavalryOfRome`.
- [x] Work on only `ZombieOrchard`.
- [x] Read source-backed runtime, interface, game, render and proof paths.
- [x] Record interaction loops, domains, kits and services.
- [x] Add timestamped persistence architecture and system audits.
- [x] Refresh required root `.agent` files.
- [x] Push only to `main`.
- [x] Update the central ledger and internal change log.
- [ ] Implement runtime or persistence changes.

## Main finding

```txt
session-select-domain-kit exists
slot-card renderer exists

but:
Entry has no Save Select route
slots are not sourced from authority
storage is absent
snapshot restore is absent
schema and migration are absent
random continuation is absent
atomic load and rollback are absent
first restored frame proof is absent
```

## Architecture decision

Document one composed domain:

```txt
zombie-orchard-versioned-persistence-authority-domain
```

It owns slot identity and indexing, envelope schemas, compatibility, durable-state projection, checksums, migrations, atomic storage results, staged load graphs, load epochs, rollback, corruption quarantine, persistence journaling and first-restored-frame acknowledgement.

## Dependency decision

Persistence remains Gate 6. It must consume the committed runtime/run identity, fixed-step tick, command transaction results, named random streams, entity sequences and durable-state fingerprint produced by Gates 1–5.

## Runtime changes

```txt
none
```

## Validation

```txt
branch created: no
pull request created: no
runtime source changed: no
npm test: not run
npm run build: not run
browser smoke: not run
persistence fixtures: unavailable
```
