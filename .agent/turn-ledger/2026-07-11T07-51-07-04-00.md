# Turn ledger — ZombieOrchard

## Timestamp

```txt
2026-07-11T07-51-07-04-00
```

## Goal

Document the composite command transaction authority needed to preserve child results, prevent nested partial publication, reject invalid targets, roll back resource/gameplay effects and correlate one committed result with one rendered frame.

## Completed

- Compared the ten accessible Publish repositories with the central ledger.
- Excluded `TheCavalryOfRome`.
- Selected only `LuminaryLabs-Publish/ZombieOrchard` under the oldest eligible rule.
- Re-read boot, runtime, composition, interface, gameplay, preset, renderer, test, build and deploy sources.
- Reconfirmed the complete interaction loop, domains, kits and services.
- Traced Construction -> Storage Shed through outer activation and nested child dispatch.
- Confirmed child-result loss, nested/double publication and independent route evaluation.
- Confirmed unknown build IDs fall back to the first catalog item.
- Confirmed Boolean-only payment, missing staging/rollback and missing command/render correlation.
- Added the timestamped tracker and architecture/system audits.
- Refreshed the root `.agent` documents and kit registry.
- Changed no runtime source.
- Used `main` only and created no branch or pull request.

## Finding

```txt
current:
  parent command
    -> public child command
    -> child publishes
    -> child result discarded
    -> parent optionally routes
    -> parent publishes

required:
  admitted transaction
    -> full preflight
    -> staged effects
    -> internal child dispatch
    -> commit or rollback
    -> one result with child evidence
    -> one publication
    -> one first-frame acknowledgement
```

## Ordered queue

```txt
1. Runtime Session Instance Authority
2. Fixed-Step Clock Authority
3. Interaction Capability Reachability
4. Composite Command Transaction Authority
5. Seeded Random and Replay Authority
6. Versioned Save / Load Authority
```

## Validation

```txt
runtime changed: no
npm test: not run
npm build: not run
browser smoke: not run
transaction fixtures: not implemented
```
